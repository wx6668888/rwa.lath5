# FE-004 设计 Token、响应式与可访问性基础验收备注

**状态：** 完成  
**完成日期：** 2026-07-11  
**设计前置：** 已在开工前完整阅读 `DESIGN.md`。本任务没有新增页面，因此复用已批准的首页概念图和锁定应用骨架作为视觉参考。

## 本次完成内容

- 新增 `lib/design-tokens.ts`，导出画布、文字、风险色、间距、移动端宽度和最小触控尺寸的类型化 Token，供应用配置和后续组件使用。
- `app/layout.tsx` 的主题色改为消费 Token，避免根级品牌色硬编码漂移。
- 在 `components/rwa-h5.tsx` 添加键盘跳过链接与 `#main-content` 主内容锚点；焦点用户可直接跳过重复导航。
- 扩展全局焦点状态覆盖链接与可聚焦元素；增加禁用按钮指针反馈和触控优化。
- 将移动端左右边距提取为 CSS 变量，保留既有 390px 紧凑断点、430px 宽度上限、桌面机框和减少动效规则。

## 验证

- 已执行 `npm run build`，通过。
- 浏览器检查：跳过链接和主内容锚点各存在一个。
- 390px 首页与 430px 钱包页无横向溢出，且品牌和四项底栏均保持可见。
- 未修改 `Brand`、`TopBar`、`BottomDock` 或详情页导航规则。

## 后续边界

- 多语言文案、阿拉伯语 RTL、真实字体加载和全量读屏脚本留在 `FE-005` 与 `QA-005`，本任务只建立它们依赖的基础能力。
