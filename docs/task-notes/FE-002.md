# FE-002 路由拆分验收备注

**状态：** 完成  
**完成日期：** 2026-07-11  
**设计前置：** 已在开工前重读 `DESIGN.md`；未改动锁定的 `Brand`、`TopBar`、`BottomDock` 与详情页导航规则。

## 本次完成内容

- 新增 `lib/rwa-routes.ts`，集中维护所有原型屏幕与 URL 的双向映射。
- 新增 `app/[[...rwa]]/page.tsx`，支持根路径和下列真实路由直接访问、刷新与深链接：
  - 一级：`/home`、`/invest`、`/portfolio`、`/wallet`、`/ai`、`/profile`。
  - 资产与订单：`/invest/rwa`、`/invest/compute`、`/invest/stocks`、`/invest/prediction`、`/orders/review`、`/orders/success`。
  - 钱包、投资组合、AI 和账户子流程：充值、提现、转账、持仓、AI 方案、通知、KYC、安全、邀请、记录、客服和设置。
- 更新 `components/rwa-h5.tsx`：现有 `go()` 在保留原型状态与滚动复位的同时，统一推送真实 URL。
- 删除旧的仅根路径 `app/page.tsx`，避免与可选动态路由冲突。

## 验证

- 已执行 `npm run build`，通过。
- Next 构建输出确认 `/[[...rwa]]` 为动态路由，`/manifest.webmanifest` 保持独立静态路由。
- 本地预览运行于 `http://localhost:3001`；`/home`、`/invest`、`/invest/prediction`、`/wallet/deposit`、`/profile/kyc` 均返回 200。
- 已在浏览器验证：390px 与 430px 视口无横向溢出，且根页面保留固定 Brand 与 BottomDock；点击底栏 Invest 后实际 URL 切换至 `/invest`。

## 审核重点

1. 打开 `/home`、`/invest`、`/portfolio`、`/wallet`、`/ai`、`/profile`，确认顶部 Logo、头像入口、四项底栏和独立 AI 按钮未变化。
2. 打开 `/invest/prediction`、`/wallet/deposit`、`/profile/kyc` 等深链并刷新，确认页面能恢复。
3. 当前原型的订单资产/金额仍主要由内存状态保存；生产 API 接入前，需要在 `API-008` 与 `FE-010` 中把订单草稿改为服务端草稿或 URL 安全参数，不能把原型状态视为生产持久化。
